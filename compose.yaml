services:
  backend:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      NEXUS_DB_PATH: /data/nexus.sqlite3
      NEXUS_SERIAL_ENABLED: "false"
      NEXUS_LOG_DIR: /data/logs
      NEXUS_ENABLE_LIVE_MODEL: ${NEXUS_ENABLE_LIVE_MODEL:-false}
      NEXUS_NEBIUS_BASE_URL: ${NEXUS_NEBIUS_BASE_URL:-}
      NEXUS_NEBIUS_API_KEY: ${NEXUS_NEBIUS_API_KEY:-}
      NEXUS_NVIDIA_MODEL: ${NEXUS_NVIDIA_MODEL:-}
      NEXUS_NEBIUS_ENABLE_THINKING: ${NEXUS_NEBIUS_ENABLE_THINKING:-}
      NEXUS_NEBIUS_MAX_OUTPUT_TOKENS: ${NEXUS_NEBIUS_MAX_OUTPUT_TOKENS:-2048}
    volumes:
      - nexus-data:/data
    restart: unless-stopped
volumes:
  nexus-data:
