# U06 integration log — 2026-09-12

## Status

`Vàng` — the integrated build is healthy, while account and live-model acceptance remain on the critical path.

- `main`: `359383fbeb2db859c4a2c8ff2b03afe376125968`
- Open pull requests: `0`
- Latest `main` CI: [nexus-ci run 34696643027](https://github.com/Little-Boy-z-NeXus/nexus-hardware-doctor/actions/runs/34696643027) — passed
- Contract: schema version `1.0.0` remains frozen; no migration is pending
- Recent integration: PRs #17–#20 merged in sequence after CI passed; frontend lint, 12 unit tests and production build passed
- Golden path impact: Dashboard and Hardware Graph receive the established REST/WebSocket snapshot contract; no device-writing behavior changed

## Blocker register

| Blocker | Impact | Owner | Resolution deadline | Escalation |
| --- | --- | --- | --- | --- |
| U02 account/credit/secret checklist is overdue and not yet evidenced. | H01 cannot prove a real Nemotron response; this blocks the first model-backed vertical slice. | Hiếu owns U02; Hoàng owns H01 execution. | 2026-09-13 09:00 ICT | If credentials or quota are unavailable, record the exact missing resource and use the controlled model-unavailable fallback for software integration only. Do not mark H01 complete. |

No blocker is currently without an owner.

## Next actions

| Order | Owner | Task | Next action | Evidence required |
| --- | --- | --- | --- | --- |
| 1 | Hiếu | U02 | Confirm all four Devpost members, representative, Nebius Builder Program access, credits and secret storage without copying any key into GitHub or the Sheet. | Checklist with secret values redacted. |
| 2 | Hoàng | H01 | After U02 is available, run one real Nemotron request and save the redacted structured response containing hypotheses, next_tool, confidence, user_message and stop_condition. | Redacted runtime log plus passing parser test. |
| 3 | Hiếu + team | U07 | Open the first vertical-slice integration PR only after H01 evidence exists; demonstrate telemetry → backend → model hypothesis → read-only tool three times. | PR, green CI and three correlated traces. |

## PR review result

There were no open pull requests at this check. The four most recent frontend PRs (#17–#20) were already merged with green CI, and local `main` matched `origin/main`.

## Technical decisions

### U06-DEC-001 — Keep contract v1 frozen

- Context: firmware, backend and frontend currently share schema version `1.0.0`.
- Decision: no field rename, removal or semantic change may merge without a migration note and coordinated consumer update.
- Consequence: new observations must first use existing optional fields or be proposed as a versioned migration.
- Owner: Hiếu; architecture review by Hoàng.
- Review: at every contract-facing PR.

### U06-DEC-002 — Green head and golden-path evidence are merge requirements

- Context: a previously green branch can become stale after new commits or conflict resolution.
- Decision: only the current PR head is eligible for merge, and the affected golden path must have evidence.
- Consequence: feature completion in a personal branch is not enough to enter `main`.
- Owner: Hiếu.
- Review: every PR.

### U06-DEC-003 — Preserve the locked hardware MVP

- Context: the accepted rig is GOOUUU ESP32-S3-N16R8 + INA226 R100 + L298N + JGB37-520.
- Decision: no additional board, sensor or driver enters the hackathon MVP without an explicit scope decision.
- Consequence: software must handle unavailable data honestly instead of inventing measurements from absent hardware.
- Owner: Hiếu + Nguyễn.
- Review: before hardware or firmware changes.

